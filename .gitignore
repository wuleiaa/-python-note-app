# Vue项目核心忽略文件
node_modules/          # 依赖包（体积大，不用上传）
dist/                  # 打包后的文件（上线才用，不用传）
build/                 # 旧版Vue打包目录
*.local.js             # 本地配置文件（比如.env.local）
.env                   # 环境变量（含敏感信息）
.env.*                 # 所有环境变量文件（除了.env.example）
!.env.example          # 保留示例环境变量

# 编辑器配置
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# 日志/缓存
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
package-lock.json      # 可选：如果用npm，可保留；用yarn则删这个
yarn.lock              # 可选：如果用yarn，可保留；用npm则删这个

# 测试文件
/tests/unit/coverage/
/tests/e2e/reports/
selenium-debug.log

# 其他
tmp/
temp/